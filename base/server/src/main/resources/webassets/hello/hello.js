var base = base || {};
base.helloController = function() {
    //TODO: the controller object needs a load function
    const controller = {
        load: function() {}
        };
    return controller;
};